node_modules/
dist/
data/
.env
# Holds this deployment's camera id. config.example.json is the template.
config.json
# Names this deployment's Worker and hostname. wrangler.example.jsonc is the template.
wrangler.jsonc
py/.venv/
py/*.pt
*.pt
*.tsbuildinfo
.DS_Store

# The generated public dashboard, and Wrangler's local state. Both are rebuilt by `ufp-speed publish`.
public/
.wrangler/

.dev.vars*
!.dev.vars.example
!.env.example
